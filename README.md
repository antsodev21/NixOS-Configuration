# Instrucciones
1. Copia el configuration.nix en /etc/nixos/configuration.nix
2. Copia las Carpetas dentro de config en ~/.config
3. Antes de copiar el .bashrc recuerda instalar OhMyBash para que el Prompt funcione con el Tema del .bashrc
